# Public API snapshot — @momics/dns-sd-shared

> Generated by `scripts/api-snapshot.ts`. Do not edit by hand.
> This is the frozen public surface (see `AGENTS.md` §3). Regenerate with
> `deno task snapshot:api` only for a deliberate, reviewed API change.

## src/index.ts

`@momics/dns-sd-shared` — the shared, runtime-agnostic foundation for a
standards-compliant DNS-SD (multicast DNS, RFC 6762 + RFC 6763) library.

This package contains the public API types, the hardened DNS wire codec, the
runtime-agnostic mDNS engine, and the two backend seams
({@link DatagramTransport} and {@link DnsSdAdapter}) that per-runtime packages
(Deno, Node.js, Tauri) plug into. It performs no I/O itself.
@module

function createDnsSd(backend: DnsSdBackend): DnsSd
  Build a {@link DnsSd} from a backend. Supply either `{ transport }` (the
  engine drives real mDNS) or `{ adapter }` (an OS resolver drives it).

function dnsSdOverAdapter(adapter: DnsSdAdapter): DnsSd
  Build a {@link DnsSd} that delegates to an OS resolver adapter.

function dnsSdOverTransport(transport: DatagramTransport, options: EngineOptions): DnsSd
  Build a {@link DnsSd} that drives the shared engine over a transport.

function encodeTxtInput(input: TxtRecordsInput | undefined): TxtAttributes
  Normalise caller-supplied TXT input into the codec's attribute form.

function fingerprint(data: Uint8Array): string
  A cheap content fingerprint (length + FNV-1a hash) used to recognise our own
  looped-back datagrams. The key is fixed-size regardless of datagram length,
  so it never allocates a datagram-sized string. Collisions only risk dropping
  a peer packet that is byte-identical to one we just sent, which does not
  occur for the distinct records each node advertises.

function instanceNameLabels(instance: string, type: string, protocol: TransportProtocol, domain): string[]
  Build a full instance-name label array, e.g. `["My Server", "_http", "_tcp", "local"]`.

function nameKey(labels: string[]): string
  Canonical, lower-cased dotted form of a name, for use as a map key.

function namesEqual(a: string[], b: string[]): boolean
  Compare two names (label arrays) case-insensitively per RFC 6762 §16.

function parseServiceName(labels: string[]): ParsedServiceName | null
  Parse a DNS-SD name (as an array of labels) into its components.

  Handles bare service types (`_http._tcp.local`), instance names
  (`Foo._http._tcp.local`) and subtype-scoped types
  (`_printer._sub._http._tcp.local`). Returns `null` if the labels do not form
  a recognisable DNS-SD name.

function serviceTypeLabels(type: string, protocol: TransportProtocol, domain): string[]
  Build the service-type name labels, e.g. `["_http", "_tcp", "local"]`.

function subtypeServiceLabels(subtype: string, type: string, protocol: TransportProtocol, domain): string[]
  Build a subtype-scoped service-type name, e.g.
  `["_printer", "_sub", "_http", "_tcp", "local"]`.

function txtFromAttributes(attributes: TxtAttributes): TxtRecords
  Convert decoded codec attributes into the public {@link TxtRecords} form.

function txtValueToString(value: TxtValue): string | true | null
  Decode a TXT value to a UTF-8 string, or `null`/`true` for the special forms.

const DEFAULT_DOMAIN: "local"
  Default DNS-SD domain.

const DEFAULT_ECHO_MAX_ENTRIES: 256
  Default cap on tracked sends, in case echoes never arrive to consume them.

const DEFAULT_ECHO_TTL_MS: 5000
  How long a remembered send stays eligible for echo suppression (ms).

const DEFAULT_TIMING: EngineTiming
  Default RFC 6762 timing.

const FAST_TIMING: EngineTiming
  Accelerated timing for deterministic in-process tests. Preserves the
  ordering and number of protocol phases while collapsing the delays.

const MDNS_IPV4: "224.0.0.251"
  The IPv4 mDNS multicast group address (RFC 6762 §3).

const MDNS_IPV6: "ff02::fb"
  The IPv6 mDNS multicast group address (RFC 6762 §3).

const MDNS_PORT: 5353
  The mDNS UDP port (RFC 6762 §3).

const SERVICE_TYPE_ENUMERATION: "_services._dns-sd._udp.local"
  The meta-query used to enumerate all service types (RFC 6763 §9).

const TTL_HOST: 120
  Default TTL (seconds) for host records that should be refreshed often.

const TTL_SHARED: 4500
  Default TTL (seconds) for shared records: PTR (RFC 6763 §6.1 recommends 75 min for these actually).

class EchoSuppressor
  Pure, bounded self-echo suppressor. Call {@link remember} for every datagram
  you send and {@link consume} for every datagram you receive: `consume`
  returns `true` (and swallows one credit) when the datagram is one of our own
  looped-back echoes.

  constructor(options: EchoSuppressorOptions)
    Create a bounded suppressor for recently sent datagrams.
  remember(data: Uint8Array): void
    Record a datagram we're about to send so its loopback echo can be dropped.
  consume(data: Uint8Array): boolean
    Whether `data` matches a datagram we recently sent (our own echo). A match
    consumes one credit, so N identical sends suppress exactly N echoes.
  clear(): void
    Forget every remembered send.

class MdnsEngine
  Runtime-agnostic mDNS engine over a {@link DatagramTransport}.

  constructor(transport: DatagramTransport, options: EngineOptions)
    Create an engine bound to a multicast datagram transport.
  browse(spec: BrowseServiceSpec): MdnsBrowser
    Start browsing for a service type.
  async advertise(spec: AdvertiseServiceSpec): Promise<MdnsResponder>
    Advertise a service; resolves once the name is claimed and announced.
  async close(): Promise<void>
    Close the engine, all browsers/responders and the transport.

interface AdapterAdvertiseHandle
  A running advertisement on an adapter. Calling {@link stop} is the "advertiseStop" op.

  readonly name: string
    The final instance name in use, which may differ from the requested name
    if the OS resolved a conflict by renaming.
  readonly fullName: string
    The final fully-qualified instance name (e.g. `Instance._http._tcp.local`),
    as reported by the OS resolver. Kept distinct from {@link name} so the
    adapter path matches the transport path's `advertise().fullName`.
  stop(): Promise<void>
    Stop this advertisement (maps to the platform's advertise-stop call).

interface AdapterBackend
  Backend that delegates to an OS resolver via an adapter (Tauri/native).

  adapter: DnsSdAdapter
    Adapter that implements browse/advertise outside the shared engine.

interface AdapterBrowseHandle
  A running browse on an adapter. Calling {@link stop} is the "browseStop" op.

  stop(): Promise<void>
    Stop this browse (maps to the platform's browse-stop call).

interface AdvertiseHandle
  A handle to a running advertisement.

  readonly name: string
    The final instance name in use. This may differ from the requested name if
    a conflict was detected and the name was automatically suffixed.
  readonly fullName: string
    The final fully-qualified instance name.
  stop(): Promise<void>
    Stop advertising, sending a goodbye packet so peers remove the service promptly.
  [[Symbol.asyncDispose]](): Promise<void>
    Async-dispose alias for {@link stop}.

interface AdvertiseOpts
  Options for {@link advertise}.

  service: AdvertiseServiceSpec
    The service to advertise.
  signal?: AbortSignal
    Optional abort signal that stops (and sends a goodbye for) the advertisement.

interface AdvertiseServiceSpec
  Describes a service to advertise.

  name: string
    The instance name, e.g. `"My Web Server"`.
  type: string
    Service type without the leading underscore, e.g. `"http"`.
  protocol: TransportProtocol
    The transport protocol the service uses.
  port: number
    The port the service listens on.
  host?: string
    Optional host name to advertise (e.g. `"my-device.local"`). Auto-derived if omitted.
  domain?: string
    Optional domain (defaults to `"local"`).
  subtypes?: string[]
    Optional DNS-SD subtypes to register for this instance.
  txt?: TxtRecordsInput
    Optional TXT attributes.

interface BrowseOpts
  Options for {@link browse}.

  service: BrowseServiceSpec
    The service type to browse for.
  timeoutMs?: number
    Optional timeout in milliseconds after which the browse generator ends.
    Omit or set to `0` for a continuous, never-ending browse.
  signal?: AbortSignal
    Optional abort signal to stop the browse early.

interface BrowseServiceSpec
  Identifies the kind of service to browse for.

  type: string
    Service type without the leading underscore, e.g. `"http"`.
  protocol: TransportProtocol
    The transport protocol the service uses.
  domain?: string
    Optional domain (defaults to `"local"`).
  subtypes?: string[]
    Optional DNS-SD subtypes to constrain the browse to.

interface DatagramSource
  The source of a received datagram.

  address: string
    The sender's IP address.
  port: number
    The sender's UDP port.
  family: IpFamily
    The sender's IP family.

interface DatagramTransport
  Abstracts a UDP socket joined to the mDNS multicast group. Implementations
  live in the per-runtime packages (e.g. `@momics/dns-sd-deno`,
  `@momics/dns-sd-node`) and in the in-memory loopback transport used for
  testing.

  The engine only requires `send`, `receive`, `localAddresses` and `close`;
  the `setMulticastTtl` / `setMulticastLoopback` hooks are optional tuning
  points the engine will use if present.

  readonly family: IpFamily
    Which IP family this transport handles.
  readonly hostname: string
    A stable host name for this machine, used to derive an advertise host.
  send(data: Uint8Array): Promise<void>
    Send a datagram to the mDNS multicast group
    (`224.0.0.251:5353` / `[ff02::fb]:5353`).
  receive(): Promise<ReceivedDatagram | null>
    Resolve with the next received datagram, or `null` once the transport is
    closed. Must never reject for ordinary end-of-stream; reserve rejection
    for genuine I/O errors.
  localAddresses(): string[]
    The set of local IP addresses belonging to this host, used to ignore our
    own multicast echoes.
  setMulticastTtl?(ttl: number): void | Promise<void>
    Set the multicast TTL (IPv4). Optional.
  setMulticastLoopback?(enabled: boolean): void | Promise<void>
    Enable/disable multicast loopback. Optional.
  close(): void | Promise<void>
    Close the socket and release resources. Idempotent.

interface DnsSd
  The public DNS-SD surface. A runtime package obtains one via
  `createDnsSd(...)` and typically re-exports its `browse` / `advertise`
  methods directly.

  browse(opts: BrowseOpts): AsyncGenerator<ServiceAnnouncement, void, void>
    Continuously discover service instances. The returned generator yields a
    {@link ServiceAnnouncement} per change until the browse is stopped (via the
    options' `signal`, the `timeoutMs`, or by returning from the loop).
  advertise(opts: AdvertiseOpts): Promise<AdvertiseHandle>
    Advertise a service on the local network.
  close(): Promise<void>
    Release the underlying backend (transport or adapter) and all resources.

interface DnsSdAdapter
  A higher-level discovery/advertisement backend backed by an OS resolver.

  The `browseStart` / `advertiseStart` methods start an operation and return a
  handle whose `stop()` performs the corresponding `browseStop` /
  `advertiseStop`. Emitted {@link ServiceAnnouncement} values must already be
  in our normalized form so the public API is identical to the transport path.

  browseStart(spec: BrowseServiceSpec, sink: ServiceSink): Promise<AdapterBrowseHandle>
    Start browsing for the given service type. Each discovery change is
    delivered to `sink` as a {@link ServiceAnnouncement}.
  advertiseStart(spec: AdvertiseServiceSpec): Promise<AdapterAdvertiseHandle>
    Start advertising the given service.
  close(): Promise<void>
    Tear down the adapter and any OS resources it holds.

interface EchoSuppressorOptions
  Options for constructing an {@link EchoSuppressor}.

  ttlMs?: number
    TTL window for a remembered send. Defaults to {@link DEFAULT_ECHO_TTL_MS}.
  maxEntries?: number
    Maximum number of distinct fingerprints to track. When exceeded, the
    oldest entry is evicted. Defaults to {@link DEFAULT_ECHO_MAX_ENTRIES}.
  now?: () => number
    Clock source (ms). Defaults to {@link Date.now}. Injectable for tests.

interface EngineOptions
  Options for constructing an {@link MdnsEngine}.

  timing?: EngineTiming
    Override the protocol timing (e.g. the accelerated preset for tests).

interface EngineTiming
  Tunable timing parameters for the engine.

  initialQueryMinMs: number
    Minimum initial delay before the first query (RFC 6762 §5.2: 20–120ms).
  initialQueryMaxMs: number
    Maximum initial delay before the first query.
  queryIntervalStartMs: number
    Interval before the second query; subsequent intervals double up to a cap.
  queryIntervalMaxMs: number
    Maximum interval between successive periodic queries (RFC 6762 §5.2: 60 min).
  probeDelayMaxMs: number
    Delay before the first probe (RFC 6762 §8.1: 0–250ms random).
  probeIntervalMs: number
    Interval between the three probes (RFC 6762 §8.1: 250ms).
  probeCount: number
    Number of probes to send before claiming a name (RFC 6762 §8.1).
  announceIntervalMs: number
    Interval between the two initial announcements (RFC 6762 §8.3: 1s).
  announceCount: number
    Number of announcements to send (RFC 6762 §8.3).
  goodbyeGraceMs: number
    How long, after a TTL=0 goodbye, to keep a record before deleting it.
  responseAggregationMinMs: number
    Minimum spacing between responses to duplicate queries (RFC 6762 §6).
  responseAggregationMaxMs: number
    Maximum random response delay for aggregation.

interface MdnsBrowser
  A live browse subscription returned by {@link MdnsEngine.browse}.

  events(): AsyncIterable<ServiceAnnouncement>
    Async stream of service lifecycle events.
  close(): void
    Stop browsing and release resources.

interface MdnsResponder
  A live advertisement returned by {@link MdnsEngine.advertise}.

  readonly name: string
    The final, possibly conflict-renamed instance name.
  readonly fullName: string
    The final fully-qualified instance name.
  stop(): Promise<void>
    Withdraw the advertisement (sending goodbye) and release resources.

interface ParsedServiceName
  A parsed DNS-SD instance or service name, split into its components.

  instance: string | null
    The instance name, or `null` for a bare service type.
  serviceType: string
    The service type without the leading underscore, e.g. `"http"`.
  protocol: TransportProtocol
    The transport protocol from the `_tcp` / `_udp` label.
  domain: string
    The trailing DNS-SD domain, e.g. `"local"`.
  subtypes: string[]
    Subtypes (without leading underscores), if the name was subtype-scoped.

interface ReceivedDatagram
  A received datagram together with where it came from.

  data: Uint8Array
    Datagram payload bytes.
  source: DatagramSource
    Address metadata for the sender.

interface ServiceAnnouncementBase
  The fields common to every {@link ServiceAnnouncement} variant, independent of
  its {@link ServiceEventKind}. The lifecycle-dependent fields (`kind`, `host`,
  `port`, `addresses`, `isActive`) are refined by each variant below.

  name: string
    The service instance name, e.g. `"My Web Server"`.
  fullName: string
    The fully-qualified instance name, e.g. `"My Web Server._http._tcp.local"`.
  serviceType: string
    The service type without the leading underscore, e.g. `"http"`.
  protocol: TransportProtocol
    The transport protocol the service runs over.
  domain: string
    The domain, e.g. `"local"`.
  subtypes: string[]
    Any DNS-SD subtypes advertised for this instance.
  txt: TxtRecords
    The instance's TXT attributes.
  lastSeenMs: number
    Wall-clock time (ms since epoch) this event was produced.

interface ServiceFound extends ServiceAnnouncementBase
  A matching instance was discovered but its `SRV` record has not been resolved
  yet, so its host/port are unknown.

  kind: "found"
    Discriminant for a newly discovered, not-yet-resolved instance.
  host: null
    Always `null` until the instance is resolved.
  port: null
    Always `null` until the instance is resolved.
  addresses: []
    Always empty until the instance is resolved.
  isActive: true
    Discovered instances are active.

interface ServiceRemoved extends ServiceAnnouncementBase
  The instance went away (goodbye packet or cache expiry). This is a teardown
  event, so `host`/`port` are informational and may be `null`.

  kind: "removed"
    Discriminant for an instance removed by goodbye or cache expiry.
  host: string | null
    The last known host name, or `null` if it was never resolved.
  port: number | null
    The last known port, or `null` if it was never resolved.
  addresses: string[]
    The last known IP addresses; may be empty.
  isActive: false
    Removed instances are no longer active.

interface ServiceResolved extends ServiceAnnouncementBase
  The instance is now resolved. Per the cross-backend contract, `host` and
  `port` are guaranteed non-null; `addresses` is best-effort (see
  {@link ServiceAnnouncement}).

  kind: "resolved"
    Discriminant for an instance whose SRV target and port are known.
  host: string
    The target host name (from the `SRV` record). Guaranteed non-null.
  port: number
    The port (from the `SRV` record). Guaranteed non-null.
  addresses: string[]
    Resolved IP addresses (IPv4 and/or IPv6). Best-effort; may be empty.
  isActive: true
    Resolved instances are active.

interface ServiceUpdated extends ServiceAnnouncementBase
  A previously resolved instance changed (e.g. a TXT or address update). Carries
  the same non-null host/port guarantee as {@link ServiceResolved}.

  kind: "updated"
    Discriminant for a resolved instance whose data changed.
  host: string
    The target host name (from the `SRV` record). Guaranteed non-null.
  port: number
    The port (from the `SRV` record). Guaranteed non-null.
  addresses: string[]
    Resolved IP addresses (IPv4 and/or IPv6). Best-effort; may be empty.
  isActive: true
    Updated instances are still active.

interface TransportBackend extends EngineOptions
  Backend that drives mDNS over a raw UDP multicast transport (Deno/Node).

  transport: DatagramTransport
    Transport the shared engine uses for mDNS datagrams.

type DnsSdBackend = TransportBackend | AdapterBackend
  A backend supplied to {@link createDnsSd}.

type IpFamily = "IPv4" | "IPv6"
  The IP family a transport operates on.

type ServiceAnnouncement = ServiceFound | ServiceResolved | ServiceUpdated | ServiceRemoved
  A single discovery event for a service instance, modelled as a discriminated
  union keyed on {@link ServiceEventKind}. Narrow on `kind` to access the
  lifecycle-dependent fields with precise types.

  The same instance is reported multiple times over its lifetime as it is
  discovered (`found`), resolved (`resolved`), changed (`updated`) and finally
  removed (`removed`).

  ## Cross-backend contract

  This contract is uniform across every backend (the pure {@link browse} engine,
  the Node transport, and the Tauri OS-resolver adapter):

  - A `resolved` or `updated` announcement guarantees non-null `host` and
    `port`. Consumers may rely on connecting to `host:port` without a null check
    after narrowing on `kind`.
  - `addresses` is best-effort: it MAY be empty even on a `resolved` /
    `updated` event. Some backends (notably the OS resolver behind the Tauri
    adapter) can legitimately deliver `host` + `port` before — or without ever —
    surfacing raw IP addresses. Do not treat an empty `addresses` array as
    "unresolved".
  - A `found` announcement always has `host: null`, `port: null` and an empty
    `addresses` array; the instance is known to exist but is not yet resolved.

type ServiceEventKind = "found" | "resolved" | "updated" | "removed"
  The kind of change an {@link ServiceAnnouncement} represents.

type ServiceSink = (event: ServiceAnnouncement) => void
  A callback the adapter invokes for each discovery event from the OS.

type TransportProtocol = "tcp" | "udp"
  The transport protocol a service runs over.

type TxtAttributes = Record<string, Uint8Array | true | null>
  Decoded TXT attribute map.

type TxtRecordInput = string | Uint8Array | true | null
  A TXT attribute value accepted from callers when advertising. In addition to
  the decoded forms, plain strings are accepted for convenience and encoded as
  UTF-8.

type TxtRecords = Record<string, TxtValue>
  Decoded TXT records, in the form exposed to callers.

type TxtRecordsInput = Record<string, TxtRecordInput>
  TXT records supplied when advertising.

type TxtValue = Uint8Array | true | null
  A TXT attribute value as returned to callers (RFC 6763 §6):

  - `true`  — the key is present with no value (bare key).
  - `null`  — the key is present with an empty value (`key=`).
  - bytes   — the key has a binary value.

## src/wire/index.ts

Standards-compliant DNS / mDNS wire codec (RFC 1035, RFC 6762, RFC 6763).
@module

function decodeMessage(bytes: Uint8Array): DnsMessage
  Decode a complete DNS message. Throws {@link WireError} if malformed.

function encodeIpv6(address: string): Uint8Array
  Encode a canonical IPv6 string (possibly with `::`) into 16 bytes.

function encodeMessage(message: DnsMessage): Uint8Array
  Encode a complete DNS message.

function isA(rr: ResourceRecord): rr is ResourceRecordA
  Whether a record is an `A` record.

function isAAAA(rr: ResourceRecord): rr is ResourceRecordAAAA
  Whether a record is an `AAAA` record.

function isNSEC(rr: ResourceRecord): rr is ResourceRecordNSEC
  Whether a record is an `NSEC` record.

function isPTR(rr: ResourceRecord): rr is ResourceRecordPTR
  Whether a record is a `PTR` record.

function isSRV(rr: ResourceRecord): rr is ResourceRecordSRV
  Whether a record is an `SRV` record.

function isTXT(rr: ResourceRecord): rr is ResourceRecordTXT
  Whether a record is a `TXT` record.

class Reader
  Bounds-checked reader for DNS wire-format fields.

  constructor(bytes: Uint8Array, offset: number)
    Create a reader at an optional absolute offset.
  readonly bytes: Uint8Array
    Message bytes being read.
  offset: number
    Current absolute read offset.
  get length(): number
    Total message length in bytes.
  get remaining(): number
    Bytes remaining from the current offset.
  u8(): number
    Read an unsigned 8-bit integer.
  u16(): number
    Read an unsigned 16-bit integer in network byte order.
  u32(): number
    Read an unsigned 32-bit integer in network byte order.
  take(count: number): Uint8Array
    Read `count` raw bytes as a copy (never a view into the message).
  peekU8(at: number): number
    Peek an unsigned byte at an absolute offset without moving the cursor.
  name(): string[]
    Decode a domain name starting at the current offset, following
    RFC 1035 §4.1.4 compression pointers. The cursor is advanced past the name
    as it appears at the current position (i.e. past the first pointer, if
    any); pointer targets are read out-of-band and do not move the cursor.

    Labels are decoded as Latin-1 (one byte per char) to preserve arbitrary
    bytes losslessly, matching the reference implementation.
  nameAt(at: number): string[]
    Decode a domain name that begins at an absolute offset (used for RDATA
    that embeds names, when reading from a scoped sub-reader is inconvenient).

class WireError extends Error
  Thrown when a DNS message cannot be decoded because it is malformed.

  constructor(message: string)
    Create a wire-format decode error.
  override readonly name: string
    Stable error name for malformed DNS wire data.

enum DnsClass
  DNS CLASS values. mDNS almost exclusively uses `IN`.

  IN
    The Internet.
  CS
    The CSNET class (obsolete).
  CH
    The CHAOS class.
  HS
    Hesiod.
  ANY
    Any class (QCLASS only).

enum Opcode
  DNS OPCODE values (RFC 1035 §4.1.1).

  Query
    Standard query.
  IQuery
    Inverse query (obsolete).
  Status
    Server status request.

enum Rcode
  DNS RCODE values (RFC 1035 §4.1.1).

  NoError
    No error condition.
  FormatError
    The name server was unable to interpret the query.
  ServerFailure
    The name server failed to process the query.
  NameError
    The domain name referenced in the query does not exist.
  NotImplemented
    The name server does not support the requested query kind.
  Refused
    The name server refused to perform the operation.

enum ResourceType
  DNS Resource Record TYPE values. Only the record types relevant to
  multicast DNS / DNS-SD are enumerated here.

  A
    A host address (IPv4).
  PTR
    A domain name pointer.
  TXT
    Text strings (key/value attributes for DNS-SD).
  AAAA
    An IPv6 host address.
  SRV
    A service location record.
  NSEC
    Authenticated proof of non-existence of record types.
  ANY
    A request for any/all records (QTYPE only).

interface DnsHeader
  A parsed DNS message header (RFC 1035 §4.1.1).

  In mDNS most flag fields are zero; the meaningful ones are `QR` (query vs.
  response) and `AA` (authoritative answer, always set on mDNS responses).

  id: number
    16-bit transaction identifier. Zero for mDNS.
  isResponse: boolean
    `false` = query, `true` = response.
  opcode: Opcode
    DNS operation code.
  authoritative: boolean
    Authoritative Answer.
  truncated: boolean
    TrunCation.
  recursionDesired: boolean
    Recursion Desired.
  recursionAvailable: boolean
    Recursion Available.
  rcode: Rcode
    DNS response code.

interface DnsMessage
  A fully decoded DNS message.

  header: DnsHeader
    DNS message header.
  questions: DnsQuestion[]
    Question section entries.
  answers: ResourceRecord[]
    Answer section records.
  authorities: ResourceRecord[]
    Authority section records.
  additionals: ResourceRecord[]
    Additional section records.

interface DnsQuestion
  A question section entry (RFC 1035 §4.1.2).

  name: string[]
    The queried name, as a sequence of labels (without a trailing empty root).
  type: ResourceType
    The requested record TYPE.
  class: DnsClass
    The requested DNS CLASS.
  unicastResponse: boolean
    mDNS "unicast response" bit (RFC 6762 §5.4). When `true` the querier
    requests a unicast (QU) rather than multicast (QM) response.

interface ResourceRecordA extends ResourceRecordBase
  An `A` record: a 4-byte IPv4 address, as an array of octets.

  type: ResourceType.A
    Discriminates an IPv4 address record.
  data: { kind: "A"; address: number[]; }
    IPv4 address RDATA.

interface ResourceRecordAAAA extends ResourceRecordBase
  An `AAAA` record: an IPv6 address, in canonical string form.

  type: ResourceType.AAAA
    Discriminates an IPv6 address record.
  data: { kind: "AAAA"; address: string; }
    IPv6 address RDATA.

interface ResourceRecordBase
  Fields common to every resource record.

  name: string[]
    The record's owner name, as a sequence of labels.
  type: ResourceType
    The record TYPE.
  class: DnsClass
    The record CLASS.
  ttl: number
    Time-to-live, in seconds. A TTL of 0 signals a "goodbye" (RFC 6762 §10.1).
  flush: boolean
    mDNS cache-flush bit (RFC 6762 §10.2). When `true`, this record replaces
    any cached records of the same name/type/class.

interface ResourceRecordNSEC extends ResourceRecordBase
  An `NSEC` record: proves which record types exist for a name (RFC 6762 §6.1).

  type: ResourceType.NSEC
    Discriminates an NSEC record.
  data: { kind: "NSEC"; nextDomainName: string[]; types: number[]; }
    NSEC next-domain and type bitmap RDATA.

interface ResourceRecordPTR extends ResourceRecordBase
  A `PTR` record: points at another name (a service instance for DNS-SD).

  type: ResourceType.PTR
    Discriminates a pointer record.
  data: { kind: "PTR"; name: string[]; }
    Target name RDATA.

interface ResourceRecordRaw extends ResourceRecordBase
  Any resource record whose RDATA we don't decode; RDATA is kept raw.

  data: { kind: "RAW"; bytes: Uint8Array; }
    Undecoded RDATA bytes.

interface ResourceRecordSRV extends ResourceRecordBase
  An `SRV` record: locates the host and port for a service instance.

  type: ResourceType.SRV
    Discriminates a service location record.
  data: { kind: "SRV"; priority: number; weight: number; port: number; target: string[]; }
    SRV priority, weight, port and target RDATA.

interface ResourceRecordTXT extends ResourceRecordBase
  A `TXT` record: an ordered map of key/value attributes (RFC 6763 §6).

  - `true`  — attribute present with no value (bare key, no `=`).
  - `null`  — attribute present with an empty value (`key=`).
  - bytes   — attribute present with a binary value (`key=<bytes>`).

  type: ResourceType.TXT
    Discriminates a TXT attribute record.
  data: { kind: "TXT"; attributes: TxtAttributes; }
    TXT attribute RDATA.

type ResourceRecord = ResourceRecordA | ResourceRecordAAAA | ResourceRecordPTR | ResourceRecordTXT | ResourceRecordSRV | ResourceRecordNSEC | ResourceRecordRaw
  A decoded resource record.

type TxtAttributes = Record<string, Uint8Array | true | null>
  Decoded TXT attribute map.

## src/testing/index.ts

Testing utilities for `@momics/dns-sd-shared`: the in-memory loopback
transport (a virtual multicast bus) and the runner-agnostic conformance
suite. Runtime packages import these to prove identical behaviour against
their own backends.
@module

function conformanceCases(): ConformanceCase[]
  Build the full set of conformance cases.

class LoopbackTransport implements DatagramTransport
  A {@link DatagramTransport} attached to a {@link VirtualBus}.

  constructor(config: LoopbackConfig)
    Attach a transport to a virtual bus configuration.
  readonly family: IpFamily
    IP family exposed by this virtual transport.
  readonly hostname: string
    Host name exposed by this virtual transport.
  readonly address: string
    Virtual unicast address used as this node's source address.
  send(data: Uint8Array): Promise<void>
    Publish a datagram on the virtual bus.
  async receive(): Promise<ReceivedDatagram | null>
    Receive the next virtual datagram, or `null` after close.
  localAddresses(): string[]
    Return this transport's virtual local address.
  close(): void
    Detach from the virtual bus and close the receive queue.

class VirtualBus
  A virtual multicast segment that fans datagrams out to attached nodes.

  packetCount: number
    Total datagrams that have crossed the bus (useful for test assertions).
  createTransport(options: { address?: string; hostname?: string; family?: IpFamily; }): LoopbackTransport
    Create a new {@link DatagramTransport} attached to this bus, with a unique
    virtual address and host name.

interface ConformanceCase
  A single conformance test case.

  name: string
    A stable, human-readable name.
  run(harness: ConformanceHarness): Promise<void>
    Run the case; throw to fail.

interface ConformanceHarness
  Supplies fresh, mutually-discoverable {@link DnsSd} nodes for one test. Each
  `run` receives its own harness on an isolated discovery segment; `cleanup`
  is invoked afterwards regardless of outcome.

  createNode(): DnsSd
    Create a new node that shares a discovery segment with this harness's other nodes.
  cleanup(): Promise<void>
    Tear down every node created by this harness.

## src/testing/harness.ts

A tiny, dependency-free cross-runtime test harness shared by every TypeScript
package in this repo. The same test files run unchanged under both
`deno run test/run.ts` and (after `tsc`) `node dist/test/run.js`.

Runtime packages import it via `@momics/dns-sd-shared/testing/harness` so that
registration, assertions and reporting are identical everywhere. Test files
may still import their own runtime APIs (`node:dgram`, `Deno.*`) — the harness
only unifies registration/assertions/reporting, not the runtime.

Tests register themselves via {@link test}; {@link runAll} executes them and
reports results, exiting non-zero on any failure.
@module

function assert(condition: unknown, message: string): asserts condition
  Assert a condition is truthy.

function assertBytesEqual(actual: Uint8Array, expected: Uint8Array, message?: string): void
  Assert two byte arrays are equal.

function assertDeepEquals(actual: unknown, expected: unknown, message?: string): void
  Assert deep structural equality (JSON-comparable values).

function assertEquals<T>(actual: T, expected: T, message?: string): void
  Assert strict (`Object.is`) equality.

async function assertThrows(fn: () => unknown, predicate?: (err: unknown) => boolean, message: string): Promise<void>
  Assert that `fn` throws (optionally matching `predicate`).

async function runAll(): Promise<void>
  Run every registered test, print a report, and exit with an appropriate code.

function test(name: string, fn: () => void | Promise<void>, options?: TestOptions): void
  Register a test case. Pass `{ ignore: true }` to register but skip it — useful
  for network-gated suites so both runtimes report the same skipped cases.

interface TestOptions
  Options accepted when registering a test.

  ignore?: boolean
    When true, the test is registered but skipped (reported as `skip`).
